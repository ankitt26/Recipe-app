<div class="" id="<%= dom_id recipe %>">
   <a href= <%= "/recipes/#{recipe.id}" %> class="recipe-div" > 
  <div class="box1">
    <h2><%= recipe.name %></h2>
    <p>By: <%= User.find(recipe.user_id).name %></p>
  </div>

  <div class="box2">
      <h3>Total food item: <span><%= recipe.recipe_foods.length %></span></h3>
      <h3>Total price: <span>$<%= RecipeFood.total_price(recipe.recipe_foods) %></span></h3>
  </div>
    </a>  
</div>
